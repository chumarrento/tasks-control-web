export interface Project {
    id: number;
    name: string;
    description: string;
    price_per_hour: string;
    formatted_price_per_hour: string;
    total: string;
    formatted_total: string;
    worked_minutes: string;
    formatted_total_duration: string;
};